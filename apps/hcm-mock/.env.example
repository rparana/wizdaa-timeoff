# apps/hcm-mock — copy to `apps/hcm-mock/.env` or export before `pnpm dev`.
# This service has no database; it only exposes HTTP stubs for HCM.

# HTTP port for the mock server (default 3001).
PORT=3001

# Optional
# NODE_ENV=development
